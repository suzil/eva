// Mount point for the command palette (Cmd+K overlay).
// Full implementation in EVA-21 / M3 polish using cmdk.
// Rendered as a sibling of AppShell so it layers above the entire shell.
export function CommandPalette() {
  return null
}
