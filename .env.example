# Eva Backend Configuration
# Copy this file to .env and fill in your values.
# Never commit .env to version control.
#
# Usage with make dev:
#   set -a && source .env && set +a && make dev
#
# Or put these values directly in backend/eva.yaml for automatic loading.

# Server port (default: 8080)
EVA_PORT=8080

# SQLite database file path (default: ./eva.db)
EVA_DB_PATH=./eva.db

# OpenAI API key — required for Agent nodes using GPT models
EVA_LLM_API_KEY=sk-...

# Anthropic API key — required for Agent nodes using Claude models
EVA_ANTHROPIC_API_KEY=sk-ant-...

# Credential master key — 32-byte hex string used for AES-256-GCM encryption
# of stored connector credentials. Generate with:
#   openssl rand -hex 32
EVA_CREDENTIAL_KEY=

# Log level: debug | info | warn | error (default: info)
EVA_LOG_LEVEL=info

# Frontend — Vite dev server proxies /api to this URL automatically.
# Only set VITE_API_URL if deploying the frontend separately from the backend.
# VITE_API_URL=http://localhost:8080
